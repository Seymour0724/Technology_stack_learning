#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<malloc.h>

int lengthOfLongestSubstring(char * s){
    int max=1;
    int sSize = sizeof(s)/sizeof(s[0]);
    for(int i=0; i<sSize; i++)
    {   
        int j = i+1;
        for(j; j<sSize; j++)
        {
            char *ss = 
        }
    }
}

int main()
{
    return 0;
}